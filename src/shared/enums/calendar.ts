export enum CalendarMode {
  MONTHLY = 'monthly',
  WEEKLY = 'weekly',
}

export enum CalendarMonthGroup {
  LAST = 'last',
  CURRENT = 'current',
  NEXT = 'next',
}

export enum DateUnit {
  WEEK = 'week',
  MONTH = 'month',
}
